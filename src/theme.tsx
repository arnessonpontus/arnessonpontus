import { createMuiTheme } from '@material-ui/core/styles';

export const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#0D4560',
      contrastText: '#fff',
    },
    secondary: {
      main: '#0F2635',
    },
    text: {
      primary: '#ffffff',
      secondary: '#00000',
    },
  },
});
